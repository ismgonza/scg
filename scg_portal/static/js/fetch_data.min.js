async function fetchData(reportId) {
    try {
        const response = await fetch(`/assets/js/security_report_id_${reportId}.json`);
        // /uploads/nessus/
        const data = await response.json();
        return data;
    } catch (error) {
        console.error('Error fetching data:', error);
        return null;
    }
}

window.fetchData = fetchData;