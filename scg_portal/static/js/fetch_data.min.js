async function fetchData(reportId) {
    try {
        const response = await fetch(`/uploads/nessus/${reportId}_ins.json`);
        // /uploads/nessus/
        const data = await response.json();
        return data;
    } catch (error) {
        console.error('Error fetching data:', error);
        return null;
    }
}

window.fetchData = fetchData;